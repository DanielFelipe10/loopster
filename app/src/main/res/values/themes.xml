<resources xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Base application theme. -->
    <style name="Theme.Looptser" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!--    imagePicker custom details-->
    <style name="CustomTheme" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="toolbarStyle">@style/Theme.Design.NoActionBar</item>
    </style>

    <style name="ThemeOverlay.App.Toolbar.Light." parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <item name="colorOnPrimary">@color/white</item>
    </style>

<!--    <style name="SmoothCheckBoxStyle.">-->
<!--        <item name="color_checked">@color/purple_500</item>-->
<!--        <item name="color_tick">@color/white</item>-->
<!--    </style>-->

      <!-- Light mode -->
      <!-- Screen background light mode -->
      <style name="SCREEN">
          <item name="android:background">#FFF</item>
      </style>

      <!-- Text light mode -->
      <style name="TEXT">
          <item name="android:textColor">#000</item>
      </style>

      <!-- Container -->
      <style name="CONTAINER">
          <item name="android:background">#DFDFDF</item>
      </style>

      <!-- Navigation bar -->
      <style name="NAVIGATION_BAR">
          <item name="android:background">@color/white</item>
          <item name="showMotionSpec">@null</item>
          <item name="hideMotionSpec">@null</item>
          <item name="itemRippleColor">@null</item>
      </style>

      <!-- Buttons on profile -->
      <style name="PROFILE_BUTTONS">
          <item name="android:background">@drawable/borders_profile</item>
      </style>

      <!-- Chat users -->
      <style name="CHAT_USER">
          <item name="android:background">@drawable/user_item_shape</item>
      </style>

      <!-- Data user -->
      <style name="DATA_USER">
          <item name="android:background">@drawable/rounded_white_popup_ui</item>
      </style>

      <!-- Divider -->
      <style name="DIVIDER">
          <item name="android:background">@color/semi_transparent</item>
      </style>

      <!-- Post -->
      <style name="POST">
          <item name="android:background">@drawable/post_item_shape</item>
      </style>

      <!-- Post -->
      <style name="INTERACTIVE_BUTTONS">
          <item name="android:background">@drawable/interactive_buttons</item>

      </style>
</resources>
