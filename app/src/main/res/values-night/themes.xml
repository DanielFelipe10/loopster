<resources xmlns:tools="http://schemas.android.com/tools" xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Base application theme. -->
    <style name="Theme.Looptser" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/black</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <!--Dark mode -->
    <!-- Screen background dark mode -->
    <style name="SCREEN">
        <item name="android:background">#323232</item>
    </style>
    <!-- Text dark mode -->
    <style name="TEXT">
        <item name="android:textColor">#FFF</item>
    </style>

    <!-- Container -->
    <style name="CONTAINER">
        <item name="android:background">#424242</item>
    </style>

    <!-- Navigation bar -->
    <style name="NAVIGATION_BAR">
        <item name="android:background">#252525</item>
        <item name="showMotionSpec">@null</item>
        <item name="hideMotionSpec">@null</item>
        <item name="itemRippleColor">@null</item>
    </style>

    <!-- Buttons on profile -->
    <style name="PROFILE_BUTTONS">
        <item name="android:background">@drawable/borders_profile_dark</item>
    </style>

    <!-- Chat users -->
    <style name="CHAT_USER">
        <item name="android:background">@drawable/user_item_shape_dark</item>
    </style>

    <style name="DATA_USER">
        <item name="android:background">@drawable/rounded_black_popup_ui</item>
    </style>

    <!-- Divider -->
    <style name="DIVIDER">
        <item name="android:background">@color/grayLight</item>
    </style>
</resources>